version: 1.0
runtime: python3
build:
  commands:
    build:
      - pip install -r requirements-production.txt
run:
  runtime-version: 3.8
  command: python production_app.py
  network:
    port: 8080
    env: PORT
  env:
    - name: PORT
      value: "8080"
